const router = {
    routes: [
        {
            path: '*',
            //redirect: '/404'
        },
        //

    ],
    /*scrollBehavior(to, from, savedPosition) {
        return { x: 0, y: 0 }
    }*/
}

export default router
